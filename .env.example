# Enter secrets only in the local, ignored .env file. Never commit credentials.
OPENAI_API_KEY=
AUTOPUBLISH=false
# Optional: public YouTube trend evidence. No OAuth tokens are used for research.
YOUTUBE_DATA_API_KEY=
